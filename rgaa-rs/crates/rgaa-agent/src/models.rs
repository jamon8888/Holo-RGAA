// Dual-model routing (ModelRouter, ModelInfo, SelectedTier) was removed in M2.6
// because the agent uses a single model, not dual-tier routing.
// If dual-model routing is needed in the future, reintroduce from git history.
