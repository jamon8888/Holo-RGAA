# False-PASS & coverage claims vs HEAD — fact table

Resolves wayfinder ticket [#184](https://github.com/jamon8888/Holo-RGAA/issues/184) (child of map #183).
Verified against HEAD of `master` = `0677195` ("fix(release): target_commitish = SHA"). Line numbers refer to that commit. Workspace = `rgaa-rs/` (per root `rgaa-rs/Cargo.toml` workspace). Target-state decisions belong to #188/#181 — this document records HEAD reality only.

Method: read committed blobs via `git show HEAD:<path>` (working tree was dirty; nothing in the working tree was consulted). Rule-name counts computed by parsing `axe_mapper.rs`, `axe_rules.json`, and `axe_mapping.json` at HEAD.

## Fact table

| # | Claim (external review / docs) | HEAD reality | file:line (at `0677195`) |
|---|---|---|---|
| 1a | `AxeMapper::map` defaults mapped criteria to `Pass` and only flips to `Fail` on a rule violation | **TRUE.** All 77 keys of the built-in map are inserted with `CriterionStatus::Pass` before any violation is examined; only a matching `violation.id` flips the entry to `Fail`. `pipeline.rs` calls it at ~610. | `rgaa-rs/crates/rgaa-rules/src/axe_mapper.rs:19-35` (Pass init at :27); fail flip `:38-52` (:42); call site `rgaa-rs/crates/rgaa-orchestrator/src/pipeline.rs:610` |
| 1b | Which rule table does runtime consult — 63-name table, `axe_rules.json`, or `axe_mapping.json`? | **The hardcoded 63-name / 77-criteria table** in `build_rgaa_to_axe_map()` (`axe_mapper.rs:64-248`). `axe_rules.json` (105 ids) is never loaded by any runtime code (only written by the `rgaa-data` generator). `axe_mapping.json` is `include_str!`-loaded into `RgaaCatalog` (`catalog.rs:7,95`) but its `axe_rules` field is metadata only — no verdict/matching code reads it (`grep '\.axe_rules'` hits: `catalog.rs:105,115,252`, `rgaa-data/src/validate.rs:82` — nothing on the audit path). | `rgaa-rs/crates/rgaa-rules/src/axe_mapper.rs:59-248`; `rgaa-rs/crates/rgaa-core/src/catalog.rs:7,95,115`; `rgaa-rs/crates/rgaa-data/src/main.rs:18-25` |
| 1c | ~40 runtime rule names absent from `axe_rules.json` can never fail | **40/63 confirmed** absent from committed `axe_rules.json` (e.g. `page-title`, `keyboard`, `doctype`, `lang`, `resize-text`, `focus-order`); the project's own generator flagged those exact names `"Invalid rules found"` and kept ≥1 validated rule for only **29/77** mapping entries (23 unique validated rules). Complementarily, 82/105 `axe_rules.json` ids are not referenced by the mapper at all → a violation on them can never map to `Fail`. Caveat: both JSON files are static data; live axe-core is fetched at runtime (`fetch_axe_source`), so "absent from `axe_rules.json`" is not itself proof the scanner can't emit the rule — but the same 40 names are marked invalid by `rgaa-data`'s validator. | `rgaa-rs/crates/rgaa-core/data/rgaa-4.1.2/axe_rules.json`; `…/axe_mapping.json` (provenance.notes); `rgaa-rs/crates/rgaa-data/src/validate.rs:16-63`; fetch `rgaa-rs/crates/rgaa-data/src/fetch.rs:16-24` |
| 2 | axe `incomplete` results → what status today? | **No status at all — dropped before mapping.** The extraction step serializes only `result.violations` (`value.get("violations")`); the `incomplete` array is discarded there. `AxeMapper` parses violation objects only; `pipeline.rs` contains zero references to "incomplete". Net effect: an axe-mapped criterion whose only signal is `incomplete` keeps its pre-seeded `Pass`. | `rgaa-rs/crates/rgaa-obscura/src/lib.rs:1433-1441` (violations-only extraction); `rgaa-rs/crates/rgaa-rules/src/axe_mapper.rs:14-15,251-257`; `rgaa-rs/crates/rgaa-orchestrator/src/pipeline.rs` (no incomplete handling) |
| 3 | `Manuel` / unmapped fallback in `audit_one` | **`Manuel` → `NeedsReview`** (`manual_status()`); **missing `PartiallyAutomatable` → `NeedsReview`**; **any other missing criterion → `NotTested`** — matching IMPLEMENTATION_PLAN §1.2 / task-1-report M1.2. Two caveats: (a) the doc comment above the loop still claims missing deterministic criteria become **"Pass"** — stale vs the code; (b) the `NotTested` branch never fires for the 77 AxeMapper-seeded criteria (which include **all 73 `Deterministe`**), because they already exist as `Pass` and the loop only fills absent keys. | `rgaa-rs/crates/rgaa-orchestrator/src/pipeline.rs:69-75` (helpers), `:681-738` (fallback loop; stale Pass comment `:683-686`; NotTested `:716-721`; Manuel `:692-705`) |
| 4a | `coverage_percent` / `calculate_compliance` computed? | **Yes, two implementations.** Per-audit: `calculate_compliance_summary` → `rgaa_report::compute_metrics`. Site-wide: `aggregate_site_compliance`. Both compute `taux_global` (C/(C+NC)), `coverage_percent` (executed validated / validated), `etat_conformite`; results land on `AuditResult`. | `pipeline.rs:77-84,481-577,766-797`; `rgaa-rs/crates/rgaa-report/src/lib.rs:100-241`; field `rgaa-rs/crates/rgaa-core/src/types.rs:81-90` |
| 4b | Exported? Any gate on export? | **Displayed, not gated.** CLI prints `Coverage: N%`; API response includes `coverage_percent`. Postgres stores `taux_global` + `etat_conformite` only (no coverage column). `validate_export` (the only "guard") checks sample size, feedback contact, proofs-per-NC, derogations — **nothing about coverage or untested criteria**, and its only callers are its own unit tests. | `rgaa-rs/crates/rgaa-cli/src/commands/analyze.rs:86`; `rgaa-rs/crates/rgaa-api/src/routes.rs:24-38`; `rgaa-rs/crates/rgaa-storage/src/postgres.rs:52,90-126`; `rgaa-rs/crates/rgaa-report/src/guard.rs:97-161` |
| 4c | `NotTested` excluded from taux denominator? | **Yes, in both math paths.** `compliance_rate`/`compute_metrics` reduce NT/NR → `NonTeste`, counted in neither C nor NC; `aggregate_site_compliance` skips mixed/NotTested and skips `Manuel` entirely. Tests pin it: `[Pass,Fail,NA,NotTested]` → 50%, `[Pass,NeedsReview]` → 100%. | `rgaa-rs/crates/rgaa-report/src/lib.rs:128-167,210-213`; `pipeline.rs:512-515,533-552,555-559`; tests `pipeline.rs:877-902`, `rgaa-report/src/lib.rs:292-299` |
| 5a | IMPLEMENTATION_PLAN §1.1 (compliance formula) | **DONE.** `C/(C+NC)` with NA/NT/NR excluded; task-1-report M1.1 records the change. | `rgaa-report/src/lib.rs:128-147`; `.superpowers/sdd/IMPLEMENTATION_PLAN/task-1-report.md` (M1.1) |
| 5b | IMPLEMENTATION_PLAN §1.2 (`audit_one` fallback → NotTested) | **DONE for the fallback branch only.** The `else` branch defaults `NotTested`; Manuel → `NeedsReview` as specified. The plan never named `AxeMapper`, whose `Pass` default is untouched — so the false-PASS surface the review described (claim 1a) is still open at HEAD. Stale "→ Pass" comment remains at `pipeline.rs:683-686`. | `pipeline.rs:716-721`; task-1-report M1.2; plan `IMPLEMENTATION_PLAN.md` §1.2 |
| 5c | m3 plan Task 5 (`coverage_percent`, partielle/retrograde) | **Landed, with divergence.** Fields + both computations exist. `compute_metrics` (per-audit path) applies `retrograde_si_non_teste` (`RGAA_41.retrograde_si_non_teste = true` → `etat_conformite = "non conforme"` when any criterion is `NotTested`); `aggregate_site_compliance` (crawl path) has **no retrograde** — thresholds only. | `rgaa-report/src/lib.rs:84-89,220-231`; `pipeline.rs:567-574`; plan `docs/superpowers/plans/2026-08-24-m3-rgaa-fidelity.md` Task 5 |
| 5d | Coverage-gated export / `incomplete → NeedsReview` (map #183 target items) | **Never landed at HEAD** — no coverage gate anywhere (4b), no incomplete handling anywhere (2). These remain open work owned by #188/#181. | `guard.rs:97-161`; `obscura/src/lib.rs:1433-1441` |
| — | "73 deterministic is partly a label" | **TRUE as a label.** `CLASSIFICATION` at HEAD: 73 `Deterministe` + 32 `IaAssiste` + 1 `Manuel` = 106. All 73 Deterministe sit inside the 77 AxeMapper pre-seeded keys, so "deterministic criterion" executes as "axe/gap didn't flag it → Pass", not as 73 individually executed checks: only 23 of the 63 consulted rule names exist in committed `axe_rules.json`, plus 10 gap-fix criteria. | `rgaa-rs/crates/rgaa-core/src/criteria.rs:18-123` (table), `:131-149` (`all()`), `axe_mapper.rs:64-248` |

## Reconciliation summary (claim → verdict)

- (a) AxeMapper defaults Pass, ~40 dead rule names → **Confirmed at HEAD** (1a/1c), with the nuance that the dead-name count is relative to committed data files, not the live axe-core fetch.
- (b) "73 deterministic" partly a label → **Confirmed** (last row): routing class, not executed coverage.
- (c) coverage_percent computed, no export gate → **Confirmed** (4a/4b).
- (d) NotTested excluded from taux denominator → **Confirmed** (4c).
- Plan/task-report claim that `audit_one` defaults NotTested → **Also true** (3, 5b) — different code path than AxeMapper; both coexist at HEAD. The external review and the plan are not in conflict; §1.2 never covered the AxeMapper pre-seed.
